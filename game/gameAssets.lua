module (..., package.seeall)

--[[
	throwable part
--]]
local throwables = require('model/throwables')

function geneThrowable( param )
	local itemGroup = layout.group()
	
	-- Type Check
	
	
	-- 

	return itemGroup
end

function release( target )
	target:removeSelf()
end

--[[
	npc part
--]]
function geneNPC( name )
	-- init npc graphic
	local npc
	
	return npc
end